.env
.env.local
.env.backup-*
.env.local.backup*
node_modules/
vendor/
.next/
storage/logs/*.log
storage/framework/cache/*
storage/framework/sessions/*
storage/framework/views/*
.idea/
.vscode/
.DS_Store
Thumbs.db
*.log

# HTTP client private envs (JetBrains / VS Code REST Client)
docs/api/http-requests/http-client.private.env.json
docs/api/http-requests/.idea/

# Backup files producidos por el script de backup-mysql.sh (en servidor)
backups/

# OpenAPI snapshot generado por CI — versionado pero no editado manualmente
# (mantener comentado hasta que CI lo regenere)
# docs/api/openapi.json

# TypeScript incremental build cache (Next.js no lo necesita versionado)
apps/web/tsconfig.tsbuildinfo

# Carpeta de frames "fuente" — los frames servidos están en apps/web/public/frames/
# Esta carpeta raíz es solo la copia local de origen
/Frames/
